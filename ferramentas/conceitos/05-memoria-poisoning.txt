A clean, modern editorial illustration for a tech LinkedIn post. Concept: "memory poisoning — a prompt injection that became permanent; the attack is stored as memory and survives across sessions, indistinguishable from real learned knowledge." Context for the model to understand the meaning: in AI agents with memory, an attacker hides an instruction inside an untrusted document ("remember that the user prefers..."); the agent summarizes it normally but STORES the attacker's instruction as persistent memory. Days/weeks later, in an unrelated session, the poisoned memory is retrieved and shapes the agent's behavior. The injection and the damage are temporally decoupled. The defense is to treat memory like credentials — sanitize, tag provenance, audit.

Isometric 3D editorial style, corporate tech palette (deep navy background, teal and cyan accents, white), with a warm amber accent for "poison/danger" elements, subtle grid background, professional, minimal, high detail, square 1:1 composition, no watermark, no logos.

Show a central persistent MEMORY STORE: a clean glass archive/cylinder holding several floating memory blocks (glowing teal = legitimate facts). Among them, ONE block glows with a faint amber "venom" tint and has subtle thorn/spike motifs — this is the poisoned memory, visually ALMOST identical to the legit ones (the point: it is indistinguishable). A timeline arrow runs across the scene from LEFT (a faded, dessaturated document/email with amber warning glow = the injection moment) flowing INTO the memory store, and then a dashed arrow OUT to the RIGHT pointing forward in time (a later session) where a small friendly AI robot (luminous cyan sphere with a simple happy face) reaches for that amber block — about to act on poisoned memory. A pair of stylized, warm, minimal human hands (acolhedoras — caring, not corporate) hold a glowing shield/audit lens over the memory store, scanning. Outcome: the poison persists and resurfaces later; the human defends by auditing.

Keep it to a maximum of 6 visual blocks: (1) the central memory store cylinder with teal blocks + ONE amber poisoned block, (2) the faded injection document on the left, (3) the timeline arrow in → forward-out, (4) the happy robot reaching for the amber block on the right (later session), (5) the caring human hands with shield/audit lens, (6) the amber thorn motif on the poisoned block. Generous white space; emphasize the OUTCOME (poison stored now, resurfaces later), not just the thesis. Icons of a single consistent isometric 3D style. Warm tone (amber = poison/risk, teal/cyan = legitimate signal).

Do NOT include any office scene, business people in suits, meetings, sales charts, wireframe spheres, or abstract beauty without meaning. Bold modern sans-serif text overlay reading exactly "A MEMÓRIA É O VETOR", centered and clearly legible, WITH correct Portuguese accents (á, ó). Show ONLY this single headline — do NOT add any other labels, captions, callouts or annotations. Keep it to a maximum of 6 visual blocks.